# UDPinger (School project)
UDP server and client for measuring packet loss and RTT

Server intentionally drops packets to test client logic

Running:
"Localhost" can be replaced in client if not running server and client on same machine.
Each program can be run in separate terminal tabs to test program


